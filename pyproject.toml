[project]
name = "create-eggscaliber-app"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "copier>=9.14.0",
    "pytest>=8.0.0",
    "pyyaml>=6.0.0",
]
